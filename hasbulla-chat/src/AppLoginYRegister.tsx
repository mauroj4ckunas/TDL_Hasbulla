import { BrowserRouter } from "react-router-dom";
import { AppRouter } from "./components/router";

function AppLoginYRegister() {
    return (
        <BrowserRouter>
        <AppRouter/>
        </BrowserRouter>
    );
}

export default AppLoginYRegister;